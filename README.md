# yubeom-vocab-app

초등학교 5학년을 위한 영어 단어 퀴즈 앱입니다.

## 기능
- 객관식 단어 퀴즈
- 연속 정답 시 난이도 자동 상승 (쉬움 → 보통 → 어려움)
- 틀린 단어 자동 수집 및 복습 모드

## 실행 방법
정적 웹앱이므로 `index.html`을 브라우저에서 열면 바로 실행됩니다.

간단히 로컬 서버로 실행하려면:

```bash
python3 -m http.server 4173
```

브라우저에서 `http://localhost:4173` 접속
